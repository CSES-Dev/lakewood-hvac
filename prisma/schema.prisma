generator client {
  provider = "prisma-client-js"
}

generator zod {
  provider = "prisma-zod-generator"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model reviews {
  id       Int    @id @default(autoincrement())
  author   String
  comments String
}
<<<<<<< HEAD
model products {
  id           Int      @id @default(autoincrement())
  image_url    String
  name         String
  status       status
  price        Decimal  @db.Decimal(10, 2)
  stock        Int
  available_at DateTime @db.Timestamp(6)
}

enum status {
  active
  inactive
  archived
}

model User {
  id       String   @id @default(cuid())
  email    String   @unique
  password String
  createdAt DateTime @default(now())
}
=======
model reviews {
  id       Int    @id @default(autoincrement())
  author   String
  comments String
}
>>>>>>> 7bfdade8c358833bb4a1666c1875eaf703ce985b
